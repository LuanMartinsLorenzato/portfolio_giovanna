export type ProjectsType = {
  client: string;
  description: string;
  en_description: string;
  images: Array<string>;
  title: string;
  thumb: string;
  link: string;
};

export type tabsType = {
  name: string;
  path: string;
};

export type SocialType = {
  name: string;
  value: string;
  img: string;
}
